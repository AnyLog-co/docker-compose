#--- General ---
# AnyLog License Key
LICENSE_KEY=""

# Information regarding which AnyLog node configurations to enable. By default, even if everything is disabled, AnyLog starts TCP and REST connection protocols
# Node Types:
#   generic: sandbox with only network configured
#   master: "blockchain" emulator
#   operator: agent dedicated to storing data
#   query: agent dedicated to querying data (enable system_query logical database)
#   publisher: agent used to distribute data
#   master-operator: a combination of master and operator on a single agent
#   master-publisher: a combination of master and publisher on a single agent
NODE_TYPE=operator

# Name of the AnyLog instance
NODE_NAME=power-plant-operator1-bkup1
# Owner of the AnyLog instance
COMPANY_NAME="Smart City"

#--- Networking ---
# Declare Policy name
CONFIG_NAME=""
# NIC type used to resolve the internal IP address (ex. eth0, lo)
NIC_TYPE=""
# Overlay IP address - if set, will replace local IP address when connecting to network
OVERLAY_IP=""
# Configurable (local) IP address that can be used when behind a proxy, or using Kubernetes for static IP
PROXY_IP=""

# Port address used by AnyLog's TCP protocol to communicate with other nodes in the network
ANYLOG_SERVER_PORT=32148
# Port address used by AnyLog's REST protocol
ANYLOG_REST_PORT=32149
# Port value to be used as an MQTT broker, or some other third-party broker
ANYLOG_BROKER_PORT=32150

# Bind settings - a true value binds to a specific IP and Port (a false value binds to all IPs)
TCP_BIND=true
REST_BIND=false
BROKER_BIND=false

# Number of concurrent threads per connection protocol
TCP_THREADS=6
REST_THREADS=6
BROKER_THREADS=6

# Timeout in seconds for REST requests (0 = continuous)
REST_TIMEOUT=30


#--- Database ---
# Physical database type (sqlite, psql)
DB_TYPE=psql
# Username for SQL database connection
DB_USER=admin
# Password correlated to database user
DB_PASSWD=passwd
# Database IP address
DB_IP=127.0.0.1
# Database port number
DB_PORT=5432
# Whether to set autocommit data
AUTOCOMMIT=true

# --- NoSQL ---
# Whether to enable NoSQL logical database
ENABLE_NOSQL=false
# Physical NoSQL database type
NOSQL_TYPE=mongo
# Username for NoSQL database connection
NOSQL_USER=""
# Password correlated to NoSQL database user
NOSQL_PASSWD=""
# NoSQL database IP address
NOSQL_IP=127.0.0.1
# NoSQL database port number
NOSQL_PORT=27017

#--- System Query ---
# Whether to start system_query logical database
SYSTEM_QUERY=false
# Database type used for system_query logical database
SYSTEM_QUERY_DB=sqlite
# Run system_query using in-memory SQLite. If set to false, will use pre-set database type
MEMORY=false

#--- Operator ---
# Member ID for Operator node - if not specified will autogenerate
# no 2 operators can have the same ID
MEMBER=""
# Owner of the cluster
CLUSTER_NAME=power-plant
# Logical database name
DEFAULT_DBMS=cos

#--- Operator specific (Advanced) ---
#--- Partitioning ---
# Whether to enable partitioning
ENABLE_PARTITIONS=true
# Table to partition (* for all tables)
TABLE_NAME=*
# Column to partition by
PARTITION_COLUMN=timestamp
# Time interval per partition
PARTITION_INTERVAL=30 days
# Number of partitions to keep
PARTITION_KEEP=3
# How often to sync partition changes
PARTITION_SYNC=1 day

#--- High Availability ---
# Whether to enable HA
ENABLE_HA=true
# How far back to pull data when HA is enabled
START_DATE=-30d

#--- Blockchain ---
# TCP connection information for Master Node
LEDGER_CONN=45.79.74.39:32048
# How often to sync from blockchain
BLOCKCHAIN_SYNC=120 second
# Source of where the blockchain data is coming from (master, optimism)
BLOCKCHAIN_SOURCE=master
# Where the local copy of the blockchain will be stored
BLOCKCHAIN_DESTINATION=file

#--- Monitoring ---
# Whether to monitor the node or not
NODE_MONITORING=true
# Store monitoring in Operator node(s)
STORE_MONITORING=true
# Accept syslog data from local (Message broker required)
SYSLOG_MONITORING=true
# Docker Monitoring
DOCKER_MONITORING=true
