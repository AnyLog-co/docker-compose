#-----------------------------------------------------------------------------------------------------------------------
# The following is intended to deploy a rest node - which is a node that configures networking but nothing else
#-----------------------------------------------------------------------------------------------------------------------
NODE_TYPE=rest
NODE_NAME=anylog-rest-node
COMPANY_NAME=AnyLog
#EXTERNAL_IP=<EXTERNAL IP>
#LOCAL_IP=<LOCAL IP>
ANYLOG_SERVER_PORT=2048
ANYLOG_REST_PORT=2049
ANYLOG_BROKER_PORT=2050
