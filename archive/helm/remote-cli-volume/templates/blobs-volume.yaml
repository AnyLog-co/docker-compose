apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: {{ .Values.general.namespace }}
  name: remote-cli-blobs-pvc
spec:
  accessModes: ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.volume.storage_size }}

