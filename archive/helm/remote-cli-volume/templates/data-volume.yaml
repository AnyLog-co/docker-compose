apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: {{ .Values.general.namespace }}
  name: remote-cli-json-pvc
spec:
  accessModes: ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.volume.storage_size }}
