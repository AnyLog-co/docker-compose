apiVersion: apps/v1
kind: Deployment
metadata:
  name: anylog-operator2
  labels:
    app: anylog-operator2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: anylog-operator2
  template:
    metadata:
      labels:
        app: anylog-operator2
    spec:
      imagePullSecrets:
      - name: imagepullsecret
      containers:
      - name: anylog-operator2
        image: anylogco/anylog-network:predevelop
        imagePullPolicy: IfNotPresent
        envFrom:
          - configMapRef:
              name: operator2-configurations
        env:
          - name: DB_PASSWD
            valueFrom:
              secretKeyRef:
                name: operator2-secret-configurations
                key: DB_PASSWD
        ports:
          - containerPort: 32158
            name: tcp
          - containerPort: 32159
            name: rest