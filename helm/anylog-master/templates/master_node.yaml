apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: anylog-master
  labels:
    app: anylog-master
spec:
  serviceName: "anylog-master"
  replicas: 1
  selector:
    matchLabels:
      app: anylog-master
  template:
    metadata:
      labels:
        app: anylog-master
    spec:
      imagePullSecrets:
      - name: imagepullsecret
      containers:
      - name: anylog-master
        image: nvcr.io/uazwwnrnci5h/anylog/anylog-network:demo
        imagePullPolicy: Always
        envFrom:
          - configMapRef:
              name: master-configurations
        ports:
          - containerPort: 32048
            name: tcp
          - containerPort: 32049
            name: rest