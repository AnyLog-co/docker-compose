apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: postgres
spec:
  type: NodePort
  ports:
  - name: postgres-tcp
    protocol: TCP
    port: 5432
    targetPort: 5432
    nodePort: 32543
  selector:
    app: postgres