apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: {{ .Values.general.namespace }}
  name: {{ .Values.volume.blobs_volume.name }}-pvc
spec:
  accessModes:
    - {{ .Values.volume.access_mode }}
  resources:
    requests:
      storage: {{ .Values.volume.storage_size }}

