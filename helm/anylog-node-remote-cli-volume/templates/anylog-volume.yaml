apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: {{ .Values.metadata.namespace }}
  name: {{ .Values.volume.anylog_volume.name }}-pvc
spec:
  accessModes:
    - {{ .Values.volume.anylog_volume.access_mode }}
  resources:
    requests:
      storage: {{ .Values.volume.anylog_volume.storage_size }}
