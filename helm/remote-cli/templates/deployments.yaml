apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f /Users/orishadmon/deployments/docker-compose/remote-cli/docker-compose.yaml
    kompose.version: 1.26.1 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: remote-cli
  name: remote-cli
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: remote-cli
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f /Users/orishadmon/deployments/docker-compose/remote-cli/docker-compose.yaml
        kompose.version: 1.26.1 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.service: remote-cli
    spec:
      containers:
        - env:
            - name: CLI_PORT
              value: "31800"
            - name: CONN_IP
              value: 0.0.0.0
          image: anylogco/remote-cli:latest
          name: remote-cli
          ports:
            - containerPort: 31800
          resources: {}
          stdin: true
          tty: true
          volumeMounts:
            - mountPath: /app/Remote-CLI/anylog_query/static/json
              name: remote-cli
      restartPolicy: Always
      volumes:
        - name: remote-cli
          persistentVolumeClaim:
            claimName: remote-cli
status: {}
