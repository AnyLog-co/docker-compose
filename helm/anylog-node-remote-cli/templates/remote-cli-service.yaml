apiVersion: v1
kind: Service
metadata:
  namespace: '{{ .Values.metadata.namespace }}'
  name: remote-cli-svs
  labels:
    app: '{{ .Values.metadata.app_name }}'
spec:
  type: NodePort
  ports:
  - name: remote-cli-port
    protocol: TCP
    appProtocol: http
    port: 31800
    targetPort: 31800
    nodePort: 31800
  selector:
    app: '{{ .Values.metadata.app_name }}'
