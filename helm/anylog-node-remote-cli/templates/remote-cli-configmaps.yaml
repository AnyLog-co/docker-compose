apiVersion: v1
kind: ConfigMap
metadata:
  namespace: '{{ .Values.metadata.namespace }}'
  name: remote-cli-configmaps
  labels:
    app: '{{ .Values.metadata.app_name }}'
data:
  CONN_IP: 0.0.0.0
  CLI_PORT: 31800
