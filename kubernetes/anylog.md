# AnyLog 

1. Login 
```shell
kubectl create secret docker-registry imagepullsecret \
  --docker-server=docker.io \
  --docker-username=oshadmon \
  --docker-password=docker4AnyLog! \
  --docker-email=oshadmon@gmail.com
```