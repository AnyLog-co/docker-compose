services:
  edgelake-${ANYLOG_TYPE}:
    image: ${IMAGE}:${TAG}
    restart: always
    env_file:
      -  ../docker-makefile/${ANYLOG_TYPE}-configs/base_configs.env
      - ../docker-makefile/${ANYLOG_TYPE}-configs/advance_configs.env
      - .env
    container_name: edgelake-${ANYLOG_TYPE}
    stdin_open: true
    tty: true
    network_mode: host
    ports:
      - ${ANYLOG_SERVER_PORT}:${ANYLOG_SERVER_PORT}
      - ${ANYLOG_REST_PORT}:${ANYLOG_REST_PORT}
      - 4242:4242/udp
    cap_add:
      - NET_ADMIN  # Adding network administration capabilities for TUN device
    devices:
      - "/dev/net/tun:/dev/net/tun"  # Adding the TUN device to the container
    volumes:
      - edgelake-${ANYLOG_TYPE}-anylog:/app/AnyLog-Network/anylog
      - edgelake-${ANYLOG_TYPE}-blockchain:/app/AnyLog-Network/blockchain
      - edgelake-${ANYLOG_TYPE}-data:/app/AnyLog-Network/data
      - edgelake-${ANYLOG_TYPE}-local-scripts:/app/deployment-scripts
      - nebula-overlay:/app/nebula
volumes:
  edgelake-${ANYLOG_TYPE}-anylog:
  edgelake-${ANYLOG_TYPE}-blockchain:
  edgelake-${ANYLOG_TYPE}-data:
  edgelake-${ANYLOG_TYPE}-local-scripts:
  nebula-overlay: